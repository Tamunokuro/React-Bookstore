import React from 'react';
import { Container, Button } from 'react-bootstrap';

const Categories = () => (
  <Container>
    <Button className="checkstatus" variant="primary">Check Status</Button>
  </Container>
);

export default Categories;
